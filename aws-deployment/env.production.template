# Production Environment Variables for AWS EC2 Deployment
# Copy this file to /home/ec2-user/app/.env on your EC2 instance

# === BASIC CONFIGURATION ===
NODE_ENV=production
PORT=3000
AWS_REGION=us-east-1
USE_AWS_SECRETS=true

# === PUBLIC CONFIGURATION ===
# Frontend URL (update with your actual frontend domain)
FRONTEND_URL=https://your-frontend-domain.com

# Firebase Project Configuration (public)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_STORAGE_BUCKET=your-firebase-project-id.appspot.com
FIREBASE_API_KEY=your-firebase-api-key
FIREBASE_AUTH_DOMAIN=your-firebase-project-id.firebaseapp.com
FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
FIREBASE_APP_ID=your-firebase-app-id
FIREBASE_MEASUREMENT_ID=your-measurement-id

# Blockchain Configuration (public)
RPC_URL=https://mainnet.infura.io/v3/your-infura-key
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/your-infura-key
CHAIN_ID=1

# Email allowed domains (public)
ALLOWED_EMAILS=admin@yourdomain.com,user@yourdomain.com

# Scheduled Jobs Configuration
CRON_SCHEDULE_DEADLINE_CHECKS=0 */10 * * * *

# === IMPORTANT NOTES ===
# 1. All sensitive secrets (private keys, passwords, API keys) are loaded from AWS Secrets Manager
# 2. Do NOT put sensitive information in this file
# 3. This file can be committed to version control as it contains only public configuration
# 4. The following secrets are automatically loaded from AWS Secrets Manager:
#    - JWT_SECRET
#    - ENCRYPTION_KEY
#    - DATABASE_ENCRYPTION_KEY
#    - SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS
#    - INFURA_API_KEY
#    - ALCHEMY_API_KEY
#    - DEPLOYER_PRIVATE_KEY
#    - BACKEND_WALLET_PRIVATE_KEY 